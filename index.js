//
// don't edit this file.  It is generated via `fes create-entries`
//
// check whether it's in sync with your lib directory via `fes checkup`
//

import { createUtility } from 'fes'

import map_definition from './lib/map'
const map = createUtility(map_definition)

export {
  map,
}
